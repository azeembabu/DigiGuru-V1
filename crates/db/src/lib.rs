//! `db` — sqlx models and the migrations runner for Digi Guru's PostgreSQL
//! schema (users, RBAC, academic hierarchy, sessions, audit log).
//!
//! Placeholder: no models or queries yet. Migrations live in `migrations/`
//! at the repo root and are owned by a separate workstream.
